{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "build": "json-server --watch db.json --port 3001"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "0.13.0"
  }
}
